

class Motif:

    def __init__(self, equation):
        assert isinstance(width, float)
        assert isinstance(height, float)
        assert isinstance(nb, int)
        self.width = width      # x
        self.height = height    # y
        self.nb = nb            # nb repetitions motifs
